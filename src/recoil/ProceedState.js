import { atom } from "recoil";

export const ProceedtState = atom({ //함수 이름으로 임포트
	key: "ProceedtState",
	default: "진행 중" // 초기값!
})

export const ProceedeState = atom({ //함수 이름으로 임포트
	key: "ProceedeState",
	default: "진행 중" // 초기값!
})